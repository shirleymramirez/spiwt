import React, { Component } from 'react';
import { Container } from 'native-base';

class SideBar extends Component {
    render() {
        return (
            <Container>
                
            </Container>
        );
    }
};

export { SideBar }